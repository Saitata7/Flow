{
  "name": "@flow/data-models",
  "version": "2.0.0",
  "main": "constants.js",
  "private": true,
  "scripts": {
    "build": "echo 'Data models package - no build needed'",
    "dev": "echo 'Data models package - no dev server needed'",
    "lint": "echo 'Linting not configured yet'",
    "test": "echo 'Testing not configured yet'",
    "type-check": "echo 'Type checking not configured yet'",
    "clean": "rm -rf node_modules/.cache"
  },
  "files": [
    "constants.js",
    "base-schema.json"
  ],
  "dependencies": {},
  "devDependencies": {}
}
